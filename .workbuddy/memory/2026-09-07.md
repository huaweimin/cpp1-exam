# 2026-09-07

## 网站迁移至腾讯云轻量服务器（备案合规）

- 背景：ICP 备案（粤ICP备2026129596号）接入商为腾讯云，但 exam-app 静态站此前部署在 Vercel（境外），存在备案核查风险（接入信息不符）。公安联网备案填报过程中发现该问题。
- 架构确认：前端静态站 + 后端 CloudBase 云函数（examSync，ap-shanghai，`tcloudbase.com`）+ PG。迁移只涉及前端静态文件，云函数不动。
- 服务器：腾讯云轻量 OpenCloudOS 9，公网 IP `203.195.188.99`，广州六区，2C2G/40G。
- 部署过程坑：
  1. 轻量新实例登录方式选了「扫码登录」，PAM 模块 `pam_qrlogin.so` 拦截 SSH 密码登录 → 注释 `/etc/pam.d/sshd` 中的 qrlogin 行 + TAT「执行命令」完成
  2. OpenCloudOS 9 密码策略最少 9 位（pwquality），`Hwm1997.`（8位）被拒 → 改用 `Hwm1997.xj`
  3. 连续试错密码触发 `pam_faillock` 锁定（deny=3），正确密码也被拒 → `faillock --user root --reset` 解锁
  4. 密码登录仍被拒（原因未深究）→ 最终用 SSH 公钥（`~/.ssh/id_ed25519.pub` 已写入 root `authorized_keys`）免密登录成功
- 部署结果：nginx 1.29.8，站点根 `/var/www/cppexam`，配置写入 `/etc/nginx/nginx.conf`（备份 `.bak`），SPA try_files 回退、assets 30 天缓存、index no-cache、gzip。SELinux disabled、firewalld inactive、80 端口公网可达（HTTP 200，约 27ms）。
- dist 说明：本地 `npm run build` 被 WorkBuddy 敏感内容保护拦截（vite loadEnv 读 .env），直接复用 9/4 构建的 dist（已含 ICP 备案号）。
- 待办：
  - [ ] 用户在 DNSPod 把 `cppexam.top`（A 76.76.21.21）和 `www`（CNAME vercel）改为 A 记录 `203.195.188.99`
  - [ ] 公安备案表单 IP 填 `203.195.188.99`，接入服务商选腾讯云
  - [ ] 服务器 root 密码已在聊天中暴露，建议尽快改（≥9位）
  - [ ] 可选：配 HTTPS（腾讯云免费证书或 certbot）
  - [ ] 可选：git 裸仓库 + post-receive hook 实现一键部署脚本
